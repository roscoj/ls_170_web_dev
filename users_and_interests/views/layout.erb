<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @title %></title>
  </head>

  <body>
    <%= yield %>
    <p>There are <%= @all_users.size %> users with a total of <%= count_interests(@all_users) %> interests.</p>
  </body>
</html>
